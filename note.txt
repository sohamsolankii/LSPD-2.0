add
	type : module in package.josn for use of import module


dotenv.config({path: './backend/.env'})  -- when runnig both server concurrently
dotenv.config({path: '.env'}) -- when runnig backend server only

// backend url: https://lspd-mq7j.onrender.com